spring:
  datasource:
    url: jdbc:mysql://localhost:3306/email_service?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver


    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: ori8591y@gmail.com
    password: lcootxacmgppwdwq  # 발급받은 앱 비밀번호 (띄어쓰기 없이!)
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

jwt:
  secret: mySecretKey123456789012345678901234567890
  # expiration: 86400000  # 토큰 만료시간 (밀리초, 24시간)

logging:
  level:
    org.hibernate.SQL: debug